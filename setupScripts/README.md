npm uninstall -g ionic

npm install -g @ionic/cli

ionic start

cd setupScripts
./setup-backend.ps1
./setup-database.sh in wsl

Start the backend server:
Development mode:
npm run dev

Production mode:
npm start
